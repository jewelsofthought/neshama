<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
<<<<<<< HEAD
    
    <!--link rel="stylesheet" href="{{ '/assets/css/index.css' | url }}"-->
    <link rel="stylesheet" href="{{ '/assets/fontawesome/fontawesome.min.css' | url }}">
    <link rel="stylesheet" href="{{ '/assets/fontawesome/solid.min.css' | url }}">
    <link rel="stylesheet" href="{{ '/assets/css/main.css' | url }}">
    <link rel="stylesheet" href="{{ '/assets/css/katex.min.css' | url }}">
    
   <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
    <!--link rel="stylesheet" href="{{ '/assets/css/debug.min.css' | url }}"-->

  </head>
  <body>
    <!--header -->
    <header>
      {% include "nav.njk" %}
    </header>

    <!--content -->
    <div class="container">

      <div class="columns is-8 layout-base">
        <main role="main" class="content{% if templateClass %} {{ templateClass }}{% endif %}">
        {{ content | safe }}
        </main>
      </div>
    </div>
  
    <!--footer -->
    {% include "footer.njk" %}

    <!-- Javascript (at end of document) -->
    <script src="assets/js/vue.min.js"></script>

    <script src="assets/js/bulma-burger-vue.js"></script>
=======
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
  </head>
  <body>
    <header>
      <h1 class="home"><a href="{{ '/' | url }}">{{ metadata.title }}</a></h1>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
      {%- endfor %}
      </ul>
    </header>

    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>

      {{ content | safe }}
    </main>

    <footer></footer>
>>>>>>> 0ef9b37 (Initial Commit)

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>
